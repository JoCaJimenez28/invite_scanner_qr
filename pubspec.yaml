name: invite_scanner_qr
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  cloud_firestore: ^5.4.4
  excel: ^4.0.6
  file_picker: ^8.1.3
  firebase_core: ^3.6.0
  flutter:
    sdk: flutter
  intl: ^0.19.0
  qr_code_scanner: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
